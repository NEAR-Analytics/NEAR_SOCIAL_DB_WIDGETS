{"tx_hash": "DBXf4HrhioPB7MwUen9xgTKTDUXjpfa6rSqoPTCmCRsj", "action_id_social": "4puCnHqecZXgNsj4kfaFRsmxZjv7kTZZRW4xCzWmcgo6-0-widget", "block_id": 85154439, "block_timestamp": "2023-02-13 17:08:53.484", "signer_id": "events_v2.near", "widget_name": "components__search_bar", "source_code": "if (!state) {\n  const fields = Object.entries(props.fields || {}).map(([key, weight]) => {\n    return { key, weight };\n  });\n  State.init({ all: props.items || [], filtered: [], term: '', fields });\n  return props.__engine.loading();\n}\n\nfunction scoreWord(word, termWord, field) {\n  if (word === termWord) {\n    return 100;\n  }\n\n  if (word.startsWith(termWord)) {\n    return 50;\n  }\n\n  if (word.endsWith(termWord)) {\n    return 25;\n  }\n\n  if (word.includes(termWord)) {\n    return 10;\n  }\n\n  return 0;\n}\n\nfunction scoreItem(item) {\n  const term = state.term.toLowerCase();\n\n  const termWords = term.split(/[,\\-_\\s]+/giu);\n\n  const fieldValues = state.fields.map((field) => {\n    return item[field.key];\n  });\n\n  const itemScores = fieldValues.map((fieldValue) => {\n    if (!fieldValue) {\n      return 0;\n    }\n\n    return fieldValue\n      .toLowerCase()\n      .split(/[,\\-_\\s]+/giu)\n      .map((word) => {\n        let score = 0;\n        for (const termWord of termWords) {\n          score += scoreWord(word, termWord, field);\n        }\n        return score;\n      })\n      .map((score) => {\n        return score * (field.weight || 1);\n      })\n      .reduce((a, b) => a + b, 0);\n  });\n\n  return itemScores.reduce((a, b) => a + b, 0);\n}\n\nfunction buildSubset(item) {\n  return state.fields.reduce((acc, field) => {\n    const value = item[field.key];\n\n    if (!value) {\n      return acc;\n    }\n\n    acc[field.key] = value;\n    return acc;\n  }, {});\n}\n\nconst items = state.all\n  .map((item) => {\n    const subset = buildSubset(item);\n    const score = scoreItem(subset);\n\n    return {\n      item,\n      score,\n      subset,\n    };\n  })\n  .sort((a, b) => {\n    return b.score - a.score;\n  })\n  .filter((item) => {\n    return item.score > 0;\n  });\n\nconst hasChanged = JSON.stringify(items) !== JSON.stringify(state.items);\n\nif (hasChanged) {\n  props.onSearch(items);\n  State.update({ items });\n}\n\nconst Searchbar = styled.input`\n  width: auto;\n  padding: 0.5rem;\n  border: 1px solid #ccc;\n  border-radius: 0;\n  outline: none;\n  font-size: 1rem;\n  margin: 8px;\n  border-radius: 8px;\n  z-index: 1;\n\n  &:focus {\n    outline: none;\n    border-color: #333;\n  }\n\n  &::placeholder {\n    color: #999;\n  }\n`;\n\nreturn (\n  <div style={{ position: 'relative' }}>\n    <Searchbar\n      onChange={(e) => {\n        const term = e.target.value;\n        State.update({ term });\n      }}\n      placeholder=\"Search for events\"\n    />\n  </div>\n);\n", "metadata": null, "branch": null, "widget_modules_used": null, "widget_url": "https://near.social/#/events_v2.near/widget/components__search_bar"}