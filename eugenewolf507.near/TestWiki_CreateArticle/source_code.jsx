return (
  <div>
    <button
      type="button"
      onClick={() => {
        console.log("Save button clicked");
      }}
    >
      Save
    </button>
    <button
      type="button"
      onClick={() => {
        console.log("Cancel button clicked");
      }}
    >
      Cancel
    </button>
  </div>
);
